namespace Library.Backend.Utils.Enum
{
    public enum CodeResponse
    {
        OK = 200,

        InternalError = 500,

        BadRequest = 102,

        NotFoundAuthor = 103,

        NotFoundCategory = 104,

        NotFoundBook = 105,
    }
}